<h1>Welcome to my blog!</h1>

<p>
  <%= link_to "Add new post", new_post_path %>

<%= Post.where(:visible => true).sort { |x,y| x.created_at <=> y.created_at }.each do |post| %>
  <%= post.title %><br />
  <%= post.body %>
  <br /><br />
<% end %>